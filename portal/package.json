{
  "name": "kliento-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "deploy": "bash build-site.sh && wrangler deploy"
  },
  "dependencies": {
    "@simplewebauthn/server": "14.0.2"
  },
  "devDependencies": {
    "@fontsource/ibm-plex-sans": "5.3.0",
    "typescript": "^5.9.0",
    "wrangler": "4.138.0"
  }
}
